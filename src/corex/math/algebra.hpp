#ifndef COREX_MATH_ALGEBRA_HPP
#define COREX_MATH_ALGEBRA_HPP

namespace cx
{
  int factorial(int n);
  int pyModInt(int x, int divisor);
  int pow(int base, int exponent);
  float pow(float base, int exponent);
}

#endif
